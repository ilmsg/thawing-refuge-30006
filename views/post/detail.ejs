<div class="row">
  <div class="col-sm-12">
    <img class="img-responsive" src="<%= getPicture(post.picture) %>" style="width:240px; padding-right:10px; float:left;"/>
    <h3><%= post.title %></h3>
    <p><%= post.description %></p>
    <p><%= post.content %></p>
    <p>
      <% post.tags.split(',').forEach((tag) => { %>
        <span class="label label-default"><%= tag %></span>
      <% }) %>
    </p>
    <p>
      <small>
        เปิดดู <i class="fa fa-eye fa-fw"></i> <%= post.view %> ครั้ง, โพสเมื่อ <%= moment(post.created_at).format('D MMMM YYYY') %>, <%= moment(post.created_at).fromNow() %>
      </small>
    </p>
  </div>
</div>

<div class="row">
  <div class="col-sm-12">
    <div id="disqus_thread"></div>
    <script>
    /**
    *  RECOMMENDED CONFIGURATION VARIABLES: EDIT AND UNCOMMENT THE SECTION BELOW TO INSERT DYNAMIC VALUES FROM YOUR PLATFORM OR CMS.
    *  LEARN WHY DEFINING THESE VARIABLES IS IMPORTANT: https://disqus.com/admin/universalcode/#configuration-variables*/
    var disqus_config = function () {
      this.page.url = '<%= current_url %>';  // Replace PAGE_URL with your page's canonical URL variable
      this.page.identifier = '<%= post._id %>'; // Replace PAGE_IDENTIFIER with your page's unique identifier variable
    };
    (function() { // DON'T EDIT BELOW THIS LINE
    var d = document, s = d.createElement('script');
    s.src = 'https://blog-uflzn7n8du.disqus.com/embed.js';
    s.setAttribute('data-timestamp', +new Date());
    (d.head || d.body).appendChild(s);
    })();
    </script>
    <noscript>Please enable JavaScript to view the <a href="https://disqus.com/?ref_noscript">comments powered by Disqus.</a></noscript>
  </div>
</div>
